<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>BookMyShow — Neon Mode</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <style>
    /* ======= BASE ======= */
    body {
      background: radial-gradient(circle at 20% 30%, #0a0a0f, #000);
      font-family: "Poppins", sans-serif;
      color: #eaeaea;
      overflow-x: hidden;
    }

    ::selection {
      background: #ff0077;
      color: #fff;
    }

    /* ======= NAVBAR ======= */
    .navbar {
      background: rgba(10, 10, 15, 0.75);
      backdrop-filter: blur(20px);
      border-bottom: 1px solid rgba(255, 0, 100, 0.15);
      padding: 1rem 1.5rem;
      box-shadow: 0 0 10px rgba(255, 0, 85, 0.1);
    }

    .navbar-brand {
      font-size: 1.8rem;
      font-weight: 700;
      color: #ff0077 !important;
      letter-spacing: 1px;
      text-shadow: 0 0 8px rgba(255, 0, 100, 0.7);
      transition: 0.3s;
    }

    .navbar-brand:hover {
      color: #ff3399 !important;
      text-shadow: 0 0 15px rgba(255, 0, 120, 1);
    }

    .nav-link {
      color: #ccc !important;
      margin-right: 1rem;
      font-weight: 500;
      transition: 0.3s;
      position: relative;
    }

    .nav-link:hover {
      color: #ff0077 !important;
      text-shadow: 0 0 8px #ff0077;
    }

    .nav-link::after {
      content: "";
      position: absolute;
      bottom: -4px;
      left: 0;
      width: 0;
      height: 2px;
      background: #ff0077;
      transition: width 0.3s;
    }

    .nav-link:hover::after {
      width: 100%;
    }

    /* ======= SEARCH ======= */
    .search-box input {
      background: rgba(255, 255, 255, 0.08);
      border: 1px solid rgba(255, 255, 255, 0.15);
      border-radius: 30px;
      padding: 8px 15px;
      color: #fff;
      transition: 0.3s;
    }

    .search-box input:focus {
      outline: none;
      border-color: #ff0077;
      box-shadow: 0 0 10px #ff0077;
    }

    .btn-search {
      background: linear-gradient(90deg, #ff0077, #ff0099);
      border: none;
      border-radius: 30px;
      color: #fff;
      font-weight: 500;
      padding: 8px 18px;
      box-shadow: 0 0 8px rgba(255, 0, 120, 0.6);
      transition: 0.3s;
    }

    .btn-search:hover {
      box-shadow: 0 0 15px #ff0099;
      transform: scale(1.05);
    }

    /* ======= SLIDER ======= */
    .slider-img {
      height: 450px;
      object-fit: cover;
      filter: brightness(0.8) contrast(1.1);
    }

    /* ======= HEADER ======= */
    .header {
      text-align: center;
      margin-top: 60px;
      margin-bottom: 30px;
    }

    .header h1 {
      font-weight: 700;
      font-size: 2.5rem;
      background: linear-gradient(90deg, #ff0077, #ff00cc);
      /* -webkit-background-clip: text; */
      -webkit-text-fill-color: transparent;
      letter-spacing: 1px;
      text-shadow: 0 0 15px rgba(255, 0, 120, 0.6);
    }

    /* ======= MOVIE GRID ======= */
    .movie-card-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 35px;
      padding: 40px 60px;
    }

    .movie-card {
      position: relative;
      background: rgba(255, 255, 255, 0.03);
      border: 1px solid rgba(255, 0, 120, 0.1);
      border-radius: 18px;
      overflow: hidden;
      box-shadow: 0 0 20px rgba(255, 0, 120, 0.08);
      transition: 0.4s;
    }

    .movie-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 0 25px rgba(255, 0, 120, 0.3);
      border-color: rgba(255, 0, 120, 0.3);
    }

    .movie-poster-wrapper {
      height: 350px;
      overflow: hidden;
    }

    .movie-poster {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: 0.5s;
    }

    .movie-card:hover .movie-poster {
      transform: scale(1.1);
    }

    .movie-rating {
      position: absolute;
      top: 15px;
      right: 15px;
      background: rgba(0, 0, 0, 0.7);
      border: 1px solid rgba(255, 215, 0, 0.4);
      padding: 6px 12px;
      border-radius: 10px;
      color: #ffd700;
      font-weight: 600;
      display: flex;
      align-items: center;
      gap: 5px;
      font-size: 0.9rem;
      box-shadow: 0 0 8px rgba(255, 215, 0, 0.3);
    }

    .movie-info {
      padding: 18px 20px;
    }

    .movie-title {
      font-size: 1.3rem;
      font-weight: 600;
      color: #fff;
      margin-bottom: 10px;
      text-shadow: 0 0 6px rgba(255, 255, 255, 0.4);
    }

    .movie-meta {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
      font-size: 0.9rem;
      color: #bbb;
    }

    .meta-item {
      background: rgba(255, 255, 255, 0.06);
      padding: 6px 10px;
      border-radius: 8px;
    }

    .movie-age {
      display: inline-block;
      background: linear-gradient(90deg, #ff0077, #ff00cc);
      color: #fff;
      font-size: 0.85rem;
      padding: 6px 12px;
      border-radius: 12px;
      font-weight: 500;
      margin-top: 12px;
      box-shadow: 0 0 8px rgba(255, 0, 120, 0.4);
    }

    /* ======= ACTION BUTTONS ======= */
    .movie-actions {
      display: flex;
      justify-content: space-between;
      padding: 15px 20px 20px;
      gap: 10px;
    }

    .btn-action {
      flex: 1;
      border: none;
      border-radius: 10px;
      padding: 10px 12px;
      color: #fff;
      font-weight: 600;
      font-size: 0.9rem;
      transition: 0.3s;
      box-shadow: 0 0 10px rgba(255, 0, 120, 0.1);
    }

    .btn-edit { background: linear-gradient(90deg, #ffc400, #ffdf6a); color: #000; }
    .btn-delete { background: linear-gradient(90deg, #ff0044, #ff2266); }
    .btn-view { background: linear-gradient(90deg, #00c896, #00f0b5); }

    .btn-edit:hover, .btn-delete:hover, .btn-view:hover {
      transform: scale(1.05);
      box-shadow: 0 0 20px rgba(255, 255, 255, 0.2);
    }

    /* ======= FOOTER ======= */
    .footer {
      text-align: center;
      background: rgba(255, 255, 255, 0.05);
      padding: 20px;
      color: #999;
      font-size: 0.9rem;
      margin-top: 60px;
      border-top: 1px solid rgba(255, 0, 120, 0.15);
      box-shadow: 0 -4px 10px rgba(255, 0, 120, 0.1);
    }

    .footer span {
      color: #ff0077;
      font-weight: 600;
      text-shadow: 0 0 8px #ff0077;
    }
  </style>
</head>

<body>
  <!-- NAVBAR -->
  <nav class="navbar navbar-expand-lg fixed-top">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><i class="fas fa-film me-2"></i>BookMyShow</a>
      <button class="navbar-toggler bg-light" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/add_movie">Add Movie</a></li>
        </ul>

        <form class="d-flex search-box" method="GET">
          <input class="form-control me-2" type="search" name="q" placeholder="Search Movies..." value="<%= searchQuery %>">
          <button class="btn btn-search"><i class="fas fa-search"></i></button>
        </form>
      </div>
    </div>
  </nav>

  <!-- SLIDER -->
  <div id="carouselExampleIndicators" class="carousel slide mt-5" data-bs-ride="carousel" data-bs-interval="3000">
    <div class="carousel-inner">
      <div class="carousel-item active">
        <img src="https://assets-in.bmscdn.com/promotions/cms/creatives/1758520057275_22ndseppostmalonedesktop.jpg" class="d-block w-100 slider-img" alt="Banner 1">
      </div>
      <div class="carousel-item">
        <img src="https://assets-in.bmscdn.com/promotions/cms/creatives/1757148047631_6thsepplaycardsep25hpptmdesktop.jpg" class="d-block w-100 slider-img" alt="Banner 2">
      </div>
      <div class="carousel-item">
        <img src="https://assets-in.bmscdn.com/promotions/cms/creatives/1759294586758_2ndoctweaponsdesktop.jpg" class="d-block w-100 slider-img" alt="Banner 3">
      </div>
    </div>
  </div>

  <!-- MOVIES SECTION -->
  <div class="header">
    <h1>Now Showing</h1>
  </div>

  <div class="movie-card-container">
    <% movies.forEach((movie) => { %>
      <div class="movie-card">
        <div class="movie-poster-wrapper">
          <img src="<%= movie.image %>" alt="<%= movie.name %>" class="movie-poster">
          <div class="movie-rating"><i class="fas fa-star"></i> <%= movie.rating %>/10</div>
        </div>

        <div class="movie-info">
          <h5 class="movie-title"><%= movie.name %></h5>
          <div class="movie-meta">
            <div class="meta-item">Status: <%= movie.status %></div>
            <div class="meta-item">Lang: <%= movie.language %></div>
            <div class="meta-item">Cat: <%= movie.category %></div>
          </div>
          <span class="movie-age">Age: <%= movie.ageValidation %></span>
        </div>

        <div class="movie-actions">
          <a href="/edit-movie/<%= movie._id %>" class="btn-action btn-edit"><i class="fas fa-edit"></i> Edit</a>
          <a href="/delete-movie/<%= movie._id %>" class="btn-action btn-delete"><i class="fas fa-trash"></i> Delete</a>
          <a href="/view-movie/<%= movie._id %>" class="btn-action btn-view"><i class="fas fa-eye"></i> View</a>
        </div>
      </div>
    <% }) %>
  </div>

  <!-- FOOTER -->
  <div class="footer">
    © 2025 <span>BookMyShow</span> — All rights reserved.
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
