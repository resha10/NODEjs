<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Movie — BookMyShow Neon</title>

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <style>
    :root {
      --bg-dark: #050505;
      --card-bg: rgba(20, 20, 30, 0.95);
      --neon-pink: #ff007f;
      --neon-blue: #00eaff;
      --neon-purple: #9d4edd;
      --text-light: #f1f1f1;
      --border-glow: 0 0 15px rgba(0, 234, 255, 0.4);
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: radial-gradient(circle at 20% 30%, #0b001a, #000);
      color: var(--text-light);
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* Navbar */
    .neon-navbar {
      background: linear-gradient(90deg, rgba(0, 0, 0, 0.9), rgba(20, 20, 40, 0.9));
      border-bottom: 2px solid var(--neon-blue);
      box-shadow: 0 0 25px rgba(0, 234, 255, 0.25);
      backdrop-filter: blur(10px);
      padding: 1rem 2rem;
    }

    .navbar-brand {
      font-size: 1.8rem;
      font-weight: 700;
      color: var(--neon-blue) !important;
      letter-spacing: 1px;
      text-shadow: 0 0 8px var(--neon-blue);
    }

    .nav-link {
      color: #ddd !important;
      margin-right: 1rem;
      font-weight: 500;
      transition: 0.3s;
    }

    .nav-link:hover {
      color: var(--neon-pink) !important;
      text-shadow: 0 0 8px var(--neon-pink);
    }

    .btn-search {
      background: linear-gradient(90deg, var(--neon-blue), var(--neon-pink));
      border: none;
      border-radius: 30px;
      color: #000;
      font-weight: 600;
      padding: 0.45rem 1.4rem;
      transition: 0.3s;
    }

    .btn-search:hover {
      transform: scale(1.05);
      box-shadow: 0 0 15px var(--neon-blue);
    }

    /* Form Container */
    .form-container {
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 70px 15px;
    }

    .form-card {
      width: 100%;
      max-width: 980px;
      background: var(--card-bg);
      border-radius: 18px;
      box-shadow: 0 0 25px rgba(0, 234, 255, 0.2);
      border: 1px solid rgba(255, 255, 255, 0.08);
      overflow: hidden;
    }

    /* Animated header */
    .form-header {
      text-align: center;
      background: linear-gradient(90deg, var(--neon-blue), var(--neon-pink), var(--neon-purple));
      background-size: 300% 300%;
      animation: gradientShift 6s ease infinite;
      padding: 2rem 1rem;
      color: #fff;
    }

    @keyframes gradientShift {
      0% {
        background-position: 0% 50%;
      }

      50% {
        background-position: 100% 50%;
      }

      100% {
        background-position: 0% 50%;
      }
    }

    .form-header h2 {
      font-weight: 700;
      font-size: 2rem;
      text-shadow: 0 0 10px #fff;
    }

    /* Form Body */
    .form-body {
      padding: 2.5rem;
    }

    .form-label {
      font-weight: 600;
      color: var(--neon-blue);
      margin-bottom: 8px;
      font-size: 1rem;
      text-shadow: 0 0 8px var(--neon-blue);
    }

    .form-control,
    .form-select {
      background-color: rgba(255, 255, 255, 0.08);
      color: #fff;
      border: 1px solid rgba(255, 255, 255, 0.2);
      border-radius: 10px;
      padding: 0.8rem 1.2rem;
      transition: 0.3s;
    }

    .form-control:focus,
    .form-select:focus {
      border-color: var(--neon-pink);
      box-shadow: 0 0 12px var(--neon-pink);
      background-color: rgba(255, 255, 255, 0.1);
    }

    .form-control::placeholder {
      color: #aaa;
    }

    .movie-icon {
      position: absolute;
      right: 15px;
      top: 50%;
      transform: translateY(-50%);
      color: var(--neon-blue);
      opacity: 0.6;
      transition: 0.3s;
    }

    .position-relative:hover .movie-icon {
      opacity: 1;
      text-shadow: 0 0 10px var(--neon-blue);
    }

    .btn-neon {
      background: linear-gradient(90deg, var(--neon-pink), var(--neon-blue));
      border: none;
      color: #000;
      border-radius: 10px;
      font-weight: 700;
      font-size: 1.1rem;
      padding: 0.9rem 2.5rem;
      letter-spacing: 1px;
      transition: 0.3s;
      box-shadow: 0 0 15px rgba(0, 234, 255, 0.3);
    }

    .btn-neon:hover {
      transform: translateY(-3px);
      box-shadow: 0 0 25px var(--neon-pink);
    }

    img.preview-img {
      border-radius: 10px;
      border: 1px solid rgba(255, 255, 255, 0.2);
      margin-top: 8px;
      transition: 0.3s;
    }

    img.preview-img:hover {
      transform: scale(1.05);
      box-shadow: 0 0 15px var(--neon-blue);
    }

    .form-footer {
      background: rgba(0, 0, 0, 0.85);
      text-align: center;
      padding: 1.2rem;
      font-size: 0.9rem;
      color: #aaa;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
    }

    .form-footer a {
      color: var(--neon-blue);
      text-decoration: none;
    }

    .form-footer a:hover {
      color: var(--neon-pink);
      text-shadow: 0 0 8px var(--neon-pink);
    }

    @media (max-width: 768px) {
      .form-body {
        padding: 1.5rem;
      }
    }
  </style>
</head>

<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg neon-navbar">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><i class="fas fa-film me-2"></i>BookMyShow</a>
      <button class="navbar-toggler text-light" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNeon"
        aria-controls="navbarNeon" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarNeon">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/add_movie">Add Movie</a></li>
        </ul>
        <form class="d-flex" role="search" method="GET">
          <input class="form-control me-2" type="search" placeholder="Search Movies..." name="q" aria-label="Search">
          <button class="btn btn-search" type="submit"><i class="fas fa-search"></i></button>
        </form>
      </div>
    </div>
  </nav>

  <!-- Form -->
  <div class="form-container">
    <div class="form-card">
      <div class="form-header">
        <h2><i class="fas fa-pen-to-square me-2"></i>Edit Movie</h2>
      </div>

      <div class="form-body">
        <form action="/edit-movie/<%= movies._id %>" method="POST" enctype="multipart/form-data" class="row g-3">

          <!-- Same inputs preserved -->
          <!-- Movie Name -->
          <div class="col-md-6 position-relative">
            <label class="form-label">Movie Name</label>
            <input type="text" class="form-control" name="name" value="<%= movies.name %>" placeholder="Enter movie name"
              required>
            <div class="movie-icon"><i class="fas fa-ticket-alt"></i></div>
          </div>

          <!-- Description -->
          <div class="col-md-6 position-relative">
            <label class="form-label">Movie Description</label>
            <input type="text" class="form-control" name="desc" value="<%= movies.desc %>"
              placeholder="Enter movie description" required>
            <div class="movie-icon"><i class="fas fa-align-left"></i></div>
          </div>

          <!-- Age -->
          <div class="col-md-4 position-relative">
            <label class="form-label">Age Validation</label>
            <input type="number" class="form-control" name="ageValidation" value="<%= movies.ageValidation %>"
              placeholder="Minimum age" min="0" max="21" required>
            <div class="movie-icon"><i class="fas fa-user-clock"></i></div>
          </div>

          <!-- Image -->
          <div class="col-md-4 position-relative">
            <label class="form-label">Movie Image</label>
            <input type="file" class="form-control" name="image" accept="image/*">
            <% if (movies.image) { %>
              <img src="<%= movies.image %>" width="120" alt="Movie Poster" class="preview-img">
            <% } %>
            <div class="movie-icon"><i class="fas fa-camera"></i></div>
          </div>

          <!-- Status -->
          <div class="col-md-4 position-relative">
            <label class="form-label">Movie Status</label>
            <select name="status" class="form-select">
              <option value="Release" <%= movies.status==='Release' ? 'selected' : '' %>>Release</option>
              <option value="Re-Release" <%= movies.status==='Re-Release' ? 'selected' : '' %>>Re-Release</option>
              <option value="Pending" <%= movies.status==='Pending' ? 'selected' : '' %>>Pending</option>
            </select>
            <div class="movie-icon"><i class="fas fa-calendar-check"></i></div>
          </div>

          <!-- Category -->
          <div class="col-md-6 position-relative">
            <label class="form-label">Movie Category</label>
            <select name="category" class="form-select">
              <option value="Animation" <%= movies.category==='Animation' ? 'selected' : '' %>>Animation</option>
              <option value="Comedy" <%= movies.category==='Comedy' ? 'selected' : '' %>>Comedy</option>
              <option value="Horror" <%= movies.category==='Horror' ? 'selected' : '' %>>Horror</option>
              <option value="Romance" <%= movies.category==='Romance' ? 'selected' : '' %>>Romance</option>
              <option value="Sports" <%= movies.category==='Sports' ? 'selected' : '' %>>Sports</option>
              <option value="Biography" <%= movies.category==='Biography' ? 'selected' : '' %>>Biography</option>
              <option value="Action" <%= movies.category==='Action' ? 'selected' : '' %>>Action</option>
              <option value="Drama" <%= movies.category==='Drama' ? 'selected' : '' %>>Drama</option>
              <option value="Adventure" <%= movies.category==='Adventure' ? 'selected' : '' %>>Adventure</option>
            </select>
            <div class="movie-icon"><i class="fas fa-tags"></i></div>
          </div>

          <!-- Language -->
          <div class="col-md-6 position-relative">
            <label class="form-label">Movie Language</label>
            <select name="language" class="form-select">
              <option value="Hindi" <%= movies.language==='Hindi' ? 'selected' : '' %>>Hindi</option>
              <option value="English" <%= movies.language==='English' ? 'selected' : '' %>>English</option>
              <option value="Gujarati" <%= movies.language==='Gujarati' ? 'selected' : '' %>>Gujarati</option>
              <option value="Tamil" <%= movies.language==='Tamil' ? 'selected' : '' %>>Tamil</option>
              <option value="Telgu" <%= movies.language==='Telgu' ? 'selected' : '' %>>Telgu</option>
              <option value="Kannada" <%= movies.language==='Kannada' ? 'selected' : '' %>>Kannada</option>
              <option value="Bhojpuri" <%= movies.language==='Bhojpuri' ? 'selected' : '' %>>Bhojpuri</option>
              <option value="Punjabi" <%= movies.language==='Punjabi' ? 'selected' : '' %>>Punjabi</option>
              <option value="Bengali" <%= movies.language==='Bengali' ? 'selected' : '' %>>Bengali</option>
            </select>
            <div class="movie-icon"><i class="fas fa-language"></i></div>
          </div>

          <!-- Duration -->
          <div class="col-md-6 position-relative">
            <label class="form-label">Duration (in minutes)</label>
            <input type="number" class="form-control" name="duration" value="<%= movies.duration %>"
              placeholder="Enter duration" min="1" required>
            <div class="movie-icon"><i class="fas fa-hourglass-half"></i></div>
          </div>

          <!-- Rating -->
          <div class="col-md-6 position-relative">
            <label class="form-label">Rating</label>
            <input type="number" step="0.1" class="form-control" name="rating" value="<%= movies.rating %>"
              placeholder="Enter rating" min="0" max="10" required>
            <div class="movie-icon"><i class="fas fa-star"></i></div>
          </div>

          <div class="col-12 text-center pt-4">
            <button type="submit" class="btn btn-neon">
              <i class="fas fa-save me-2"></i>Update Movie
            </button>
          </div>

        </form>
      </div>

      <div class="form-footer">
        <p>© 2025 Neon Cinemas | <a href="#">Premium Experience</a></p>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
