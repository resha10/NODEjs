<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= movie.name %> | BookMyShow</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap');

    body {
      font-family: 'Poppins', sans-serif;
      background: radial-gradient(circle at top right, #0f0f1a, #000);
      color: #fff;
      margin: 0;
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* ===== HEADER ===== */
    .bms-header {
      background: rgba(15, 15, 25, 0.9);
      border-bottom: 1px solid rgba(255, 0, 85, 0.3);
      backdrop-filter: blur(15px);
      box-shadow: 0 0 25px rgba(255, 0, 85, 0.2);
      padding: 1rem 2rem;
      position: sticky;
      top: 0;
      z-index: 1000;
    }

    .bms-header .navbar-brand {
      color: #ff006e !important;
      font-weight: 700;
      font-size: 1.8rem;
      letter-spacing: 1px;
      text-shadow: 0 0 10px #ff006e;
    }

    .bms-header .nav-link {
      color: #ccc !important;
      font-weight: 500;
      margin-right: 20px;
      transition: all 0.3s ease;
      position: relative;
    }

    .bms-header .nav-link:hover {
      color: #00f5ff !important;
      text-shadow: 0 0 10px #00f5ff;
    }

    /* ===== MOVIE DETAIL SECTION ===== */
    .movie-detail-section {
      display: flex;
      flex-wrap: wrap;
      align-items: center;
      justify-content: center;
      padding: 70px 10%;
      gap: 60px;
      position: relative;
      overflow: hidden;
    }

    .movie-detail-section::before {
      content: "";
      position: absolute;
      top: -100px;
      left: -100px;
      width: 400px;
      height: 400px;
      background: radial-gradient(circle, rgba(255, 0, 85, 0.4), transparent 70%);
      filter: blur(100px);
      z-index: 0;
    }

    .movie-poster {
      flex: 1 1 350px;
      max-width: 420px;
      border-radius: 25px;
      overflow: hidden;
      box-shadow: 0 0 25px rgba(255, 0, 85, 0.5);
      transition: all 0.5s ease;
      position: relative;
      z-index: 1;
    }

    .movie-poster img {
      width: 100%;
      border-radius: 25px;
      transition: transform 0.5s ease, filter 0.4s ease;
    }

    .movie-poster:hover img {
      transform: scale(1.05);
      filter: brightness(1.2);
    }

    .movie-details {
      flex: 1 1 500px;
      background: rgba(255, 255, 255, 0.05);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 25px;
      padding: 45px;
      backdrop-filter: blur(20px);
      box-shadow: 0 0 40px rgba(0, 255, 255, 0.2);
      position: relative;
      z-index: 1;
    }

    .movie-details h1 {
      font-size: 2.5rem;
      font-weight: 700;
      color: #00f5ff;
      margin-bottom: 15px;
      text-shadow: 0 0 15px #00f5ff;
    }

    .movie-age {
      display: inline-block;
      background: linear-gradient(90deg, #ff006e, #ff4c75);
      color: #fff;
      font-weight: 600;
      border-radius: 20px;
      padding: 6px 14px;
      font-size: 0.9rem;
      margin-bottom: 25px;
      box-shadow: 0 0 10px #ff4c75;
    }

    .movie-desc {
      color: #bbb;
      font-size: 1.05rem;
      line-height: 1.7;
      margin-bottom: 30px;
    }

    .movie-meta {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 20px;
      margin-bottom: 30px;
    }

    .meta-card {
      background: rgba(0, 0, 0, 0.6);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 15px;
      padding: 15px;
      text-align: center;
      transition: all 0.3s ease;
    }

    .meta-card:hover {
      box-shadow: 0 0 20px rgba(0, 255, 255, 0.3);
      transform: translateY(-3px);
    }

    .meta-label {
      font-size: 0.85rem;
      color: #aaa;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .meta-value {
      color: #fff;
      font-weight: 600;
      font-size: 1rem;
      margin-top: 4px;
      text-shadow: 0 0 10px #00f5ff;
    }

    .movie-actions {
      display: flex;
      gap: 20px;
      margin-top: 30px;
    }

    .btn-bms {
      flex: 1;
      padding: 14px;
      font-weight: 600;
      font-size: 1.1rem;
      border-radius: 14px;
      border: none;
      transition: all 0.4s ease;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .btn-book {
      background: linear-gradient(90deg, #ff006e, #ff4c75);
      color: #fff;
      box-shadow: 0 0 20px rgba(255, 0, 85, 0.5);
    }

    .btn-book:hover {
      box-shadow: 0 0 35px rgba(255, 0, 85, 0.9);
      transform: scale(1.05);
    }

    .btn-back {
      background: linear-gradient(90deg, #00f5ff, #00d4ff);
      color: #000;
      box-shadow: 0 0 20px rgba(0, 255, 255, 0.5);
    }

    .btn-back:hover {
      box-shadow: 0 0 35px rgba(0, 255, 255, 0.9);
      transform: scale(1.05);
    }

    /* ===== FOOTER ===== */
    .bms-footer {
      text-align: center;
      padding: 30px;
      background: #0a0a0f;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
      color: #777;
      font-size: 0.95rem;
      margin-top: 80px;
    }

    .bms-footer span {
      color: #00f5ff;
      text-shadow: 0 0 10px #00f5ff;
    }

    @media (max-width: 768px) {
      .movie-detail-section {
        flex-direction: column;
        padding: 50px 25px;
      }

      .movie-details {
        padding: 30px;
      }

      .movie-details h1 {
        font-size: 2rem;
      }
    }
  </style>
</head>

<body>

  <!-- HEADER -->
  <nav class="navbar navbar-expand-lg bms-header">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><i class="fas fa-film me-2"></i>BookMyShow</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarBMS">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarBMS">
        <ul class="navbar-nav me-auto">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- MOVIE DETAILS -->
  <section class="movie-detail-section">
    <div class="movie-poster">
      <img src="<%= movie.image %>" alt="<%= movie.name %>">
    </div>

    <div class="movie-details">
      <h1><%= movie.name %></h1>
      <div class="movie-age">Age: <%= movie.ageValidation %></div>

      <p class="movie-desc"><%= movie.desc %></p>

      <div class="movie-meta">
        <div class="meta-card">
          <div class="meta-label">Status</div>
          <div class="meta-value"><%= movie.status %></div>
        </div>
        <div class="meta-card">
          <div class="meta-label">Language</div>
          <div class="meta-value"><%= movie.language %></div>
        </div>
        <div class="meta-card">
          <div class="meta-label">Category</div>
          <div class="meta-value"><%= movie.category %></div>
        </div>
        <div class="meta-card">
          <div class="meta-label">Rating</div>
          <div class="meta-value"><i class="fas fa-star text-warning"></i> <%= movie.rating %>/10</div>
        </div>
      </div>

      <div class="meta-card" style="margin-bottom: 20px; text-align:center;">
        <div class="meta-label">Movie Timing</div>
        <div class="meta-value"><i class="fas fa-clock"></i> <%= movie.duration %></div>
      </div>

      <div class="movie-actions">
        <a href="/" class="btn-bms btn-book">Book Now</a>
        <a href="/" class="btn-bms btn-back">Back to Movies</a>
      </div>
    </div>
  </section>

  <!-- FOOTER -->
  <div class="bms-footer">
    <p>© 2025 <span>BookMyShow</span> | Designed for movie lovers 🎬</p>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
