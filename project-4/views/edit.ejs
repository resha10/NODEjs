<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Book</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background: linear-gradient(120deg, #6a11cb 0%, #2575fc 100%);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: "Poppins", sans-serif;
      padding: 20px;
    }

    .form-container {
      backdrop-filter: blur(10px);
      background: rgba(255, 255, 255, 0.15);
      border-radius: 20px;
      padding: 40px;
      width: 90%;
      max-width: 550px;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.25);
      color: #fff;
      transition: all 0.3s ease-in-out;
    }

    .form-container:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 35px rgba(0, 0, 0, 0.35);
    }

    h2 {
      text-align: center;
      font-weight: 600;
      margin-bottom: 30px;
      letter-spacing: 1px;
    }

    label {
      font-weight: 500;
      margin-bottom: 6px;
    }

    .form-control,
    .form-select {
      background-color: rgba(255, 255, 255, 0.2);
      color: #fff;
      border: none;
      border-radius: 10px;
      padding: 10px 15px;
      transition: all 0.3s;
    }

    .form-control::placeholder {
      color: rgba(255, 255, 255, 0.7);
    }

    .form-control:focus,
    .form-select:focus {
      background-color: rgba(255, 255, 255, 0.3);
      box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.4);
    }

    .btn-primary {
      background: linear-gradient(90deg, #00c6ff, #0072ff);
      border: none;
      border-radius: 10px;
      padding: 10px;
      font-weight: 600;
      transition: all 0.3s;
    }

    .btn-primary:hover {
      background: linear-gradient(90deg, #0072ff, #00c6ff);
      transform: translateY(-2px);
    }

    .btn-secondary {
      background-color: rgba(255, 255, 255, 0.2);
      border: none;
      border-radius: 10px;
      font-weight: 600;
      color: #fff;
      transition: all 0.3s;
    }

    .btn-secondary:hover {
      background-color: rgba(255, 255, 255, 0.35);
    }

    select option {
      color: #000;
    }

    @media (max-width: 576px) {
      .form-container {
        padding: 30px 25px;
      }

      h2 {
        font-size: 1.5rem;
      }
    }
  </style>
</head>

<body>
  <div class="form-container">
    <h2>✏️ Edit Book Details</h2>

    <form method="post" action="/updateBook">
      <input type="hidden" name="editid" value="<%= data.id %>">

      <div class="mb-3">
        <label for="book_name" class="form-label">Book Name</label>
        <input type="text" class="form-control" id="book_name" name="book_name"
          value="<%= data.book_name %>" placeholder="Enter book title" required>
      </div>

      <div class="mb-3">
        <label for="book_author" class="form-label">Author</label>
        <input type="text" class="form-control" id="book_author" name="book_author"
          value="<%= data.book_author %>" placeholder="Enter author's name" required>
      </div>

      <div class="mb-3">
        <label for="book_price" class="form-label">Price (₹)</label>
        <input type="number" class="form-control" id="book_price" name="book_price"
          value="<%= data.book_price %>" placeholder="Enter price" required>
      </div>

      <div class="mb-3">
        <label for="book_pages" class="form-label">Page Range</label>
        <select class="form-select" id="book_pages" name="book_pages" required>
          <option disabled>--- Select Page Range ---</option>
          <% const pagesOptions = ["50 - 100 Pages", "101 - 200 Pages", "201 - 300 Pages", "301 - 500 Pages", "500+ Pages"]; pagesOptions.map((pages) => { %>
            <option <% if(data.book_pages == pages) { %> selected <% } %> value="<%= pages %>">
              <%= pages %>
            </option>
          <% }) %>
        </select>
      </div>

      <button type="submit" class="btn btn-primary w-100">Update Book</button>
    </form>

    <div class="text-center mt-3">
      <a href="/" class="btn btn-secondary w-100">Back to View Books</a>
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
